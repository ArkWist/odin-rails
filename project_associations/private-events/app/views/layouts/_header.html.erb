<header>
  <nav>
    <ul>
      <% if logged_in? %>
        <li>Logged in as <%= current_user.name %></li>
      <% else %>
        <li><%= link_to "Log in", login_path %></li>
      <% end %>
    </ul>
  </nav>
</header>